
<!--adding card view instead of frame layout solves the problem of not rendering when in landscape mode and item clicked from
 main activity also in landscape mode. Reason being grid view cannot solve the constraints been given to it. Reason being Grid layout
 requires being inline toolbar rather than over it-->

<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/forecast_fragment_container"
    android:name="com.example.android.sunshine.DetailFragment"
    android:layout_width="match_parent"
    android:layout_height="match_parent"

    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:layout="@layout/fragment_detail"
    />






